import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Search" };

export default function SearchPage() {
  return (
    <PagePlaceholder
      title="Search"
      description="Search across projects, people and open roles."
    />
  );
}
