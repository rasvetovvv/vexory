import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Open Roles" };

export default function RolesPage() {
  return (
    <PagePlaceholder
      title="Open Roles"
      description="Roles across all projects: equity, paid, contract or volunteer — join a team that's shipping."
    />
  );
}
