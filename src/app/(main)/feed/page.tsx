import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Feed" };

export default function FeedPage() {
  return (
    <PagePlaceholder
      title="Feed"
      description="All activity tied to real product work: launches, build log updates, new roles and funding."
    />
  );
}
