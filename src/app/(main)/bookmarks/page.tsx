import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Bookmarks" };

export default function BookmarksPage() {
  return (
    <PagePlaceholder
      title="Bookmarks"
      description="Projects you saved to check on later."
    />
  );
}
